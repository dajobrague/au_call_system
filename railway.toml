[build]
builder = "NIXPACKS"
buildCommand = "npm install --prefix voice-agent"
watchPatterns = ["voice-agent/**"]

[deploy]
startCommand = "node voice-agent/websocket-server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

