[build]
builder = "NIXPACKS"
buildCommand = "cd voice-agent && npm install"

[deploy]
startCommand = "cd voice-agent && node websocket-server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

