name = "voice-proxy"
main = "src/index.ts"
compatibility_date = "2024-10-01"
workers_dev = true
account_id = "b0b99c27c22e012d20d20ebe9e910f39"

# Custom domain configuration for Twilio compatibility
# Note: *.workers.dev URLs don't work directly with Twilio Media Streams
# Custom domain (sam.netmtion.io) will be configured in Cloudflare dashboard
# After deployment, add custom domain in: Workers & Pages > voice-proxy > Settings > Triggers

[[durable_objects.bindings]]
name = "CALL_SESSIONS"
class_name = "CallSession"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["CallSession"]
