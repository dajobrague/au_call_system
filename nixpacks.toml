[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["cd voice-agent && npm install"]

[phases.build]
cmds = ["cd voice-agent && npm run build"]

[start]
cmd = "cd voice-agent && node server.js"

